---
layout: layouts/base.njk
title: Commandant's Challenge
subtitle: coming soon...
---
{# <h2 class="text-3xl border-b-2">This site is a starting point</h2> #}

{# <p>You can take this template and create whatever you want. There are some useful snippets of HTML and great features built right in.
Modifying it to fit a brochure site is as simple as copying over your assets and copy.</p> #}
<div class="flex-wrap bg-blue-800 text-center h-screen text-blue-100">
  <h1 class="flex-1 py-64 font-titlescript text-6xl">{{ title }}</h1>
  {%- if subtitle %}<p class="flex-1 font-titlescript italic text-2xl">{{ subtitle | safe }}</p>{% endif %}
</div>

<div class="features sm:flex sm:justify-around">
  <div class="sm:w-1/3 p-8 text-center bg-blue-300">
    <p class="font-semibold">Mind</p>
    {# <time datetime="{{ page.date }}">{{ page.date | dateDisplay("LLLL d, y") }}</time> #}
  </div>
  <div class="sm:w-1/3 p-8 text-center bg-green-700">
    <p class="font-semibold">Body</p>
    {# <time datetime="{{ page.date }}">{{ page.date | dateDisplay("LLLL d, y") }}</time> #}
  </div>
  <div class="sm:w-1/3 p-8 text-center bg-yellow-400">
    <p class="font-semibold">Spirit</p>
    {# <time datetime="{{ page.date }}">{{ page.date | dateDisplay("LLLL d, y") }}</time> #}
  </div>
</div>

{# <p>Also includes support for Netlify Functions, because they are awesome for getting work done.</p>
<ul>
  <li><a href="/api/hello">/api/hello</a></li>
</ul> #}